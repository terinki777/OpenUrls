# Pause before OPEN the browser (seconds). Pause = (rand.max.sec - rand.min.sec) + rand.min.sec)
open.min.sec=3
open.max.sec=5
# Pause before CLOSE the browser (seconds). Pause = (rand.max.sec - rand.min.sec) + rand.min.sec)
close.min.sec=5
close.max.sec=10
# The number of browser restarts
rerun.count=10
# browser size
browser.size=1024x768
