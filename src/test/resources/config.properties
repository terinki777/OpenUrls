# Pause before open the browser (seconds). Pause = (rand.max.sec - rand.min.sec) + rand.min.sec)
rand.min.sec=3
rand.max.sec=5
# Pause before closing the browser (seconds)
timeout.in.seconds=360
# The number of browser restarts
rerun.count=10
# browser size
browser.size=1024x768
